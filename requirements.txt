# =============================================================================
# GOV-AI 2.0 — Dépendances Python (Sprint 1)
# Python >= 3.11
# Versions validées pour la cohérence inter-dépendances.
# =============================================================================

# --- Web framework ---
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.18
httpx>=0.27.0,<0.29.0

# --- Pydantic / Settings ---
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# --- Base de données ---
SQLAlchemy>=2.0.0,<3.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
greenlet>=3.0.0

# --- Milvus (Vector DB) ---
pymilvus>=2.4.0,<3.0.0

# --- Elasticsearch (BM25) ---
elasticsearch[async]>=8.13.0,<9.0.0

# --- LLM / Embeddings ---
# Ollama >= 0.4.0 : compatible httpx >= 0.27 sans plafond <0.28
ollama>=0.4.0
openai>=1.40.0,<2.0.0

# sentence-transformers, torch, transformers : optionnels (provider=local)
# Décommenter uniquement pour déploiement avec modèles locaux HuggingFace :
# sentence-transformers>=3.0.0
# torch>=2.1.0
# transformers>=4.40.0
# accelerate>=0.30.0
# tokenizers>=0.19.0

huggingface-hub>=0.24.0

# --- LangChain / LangGraph ---
# Versions cohérentes entre elles (réseau de dépendances strict)
langchain>=0.3.7,<0.4.0
langchain-core>=0.3.15,<0.4.0
langchain-community>=0.3.7,<0.4.0
langgraph>=0.2.40

# --- Traitement documents ---
PyMuPDF>=1.23.0,<2.0.0
pytesseract>=0.3.10
pillow>=10.0.0
pdf2image>=1.16.0

# --- NLP ---
langdetect>=1.0.9
tiktoken>=0.7.0

# --- Sécurité ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.9

# --- Monitoring / Logging ---
structlog>=24.0.0
python-json-logger>=2.0.7
prometheus-client>=0.20.0

# --- Utilitaires ---
python-dotenv>=1.0.0
click>=8.1.0
rich>=13.0.0
tqdm>=4.65.0
aiofiles>=23.0.0
tenacity>=8.2.0
numpy>=1.26.0,<2.0.0
pandas>=2.1.0,<3.0.0
orjson>=3.9.0
PyYAML>=6.0
regex>=2023.0.0

# --- HTTP / Async ---
httpcore>=1.0.0
aiohttp>=3.9.0
anyio>=4.0.0
sniffio>=1.3.0

# --- Autres ---
sse-starlette>=2.1.0
typing_extensions>=4.8.0
requests>=2.31.0
